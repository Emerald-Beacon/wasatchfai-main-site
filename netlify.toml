# Netlify configuration for Wasatch FAI Website
# Static HTML site - no build required

[build]
  publish = "."

# ---------------------------------------------------------------------------
# CUTOVER COMPLETE -- 2026-08-03
#
# wasatchfai.com is now served by Netlify from this repo. The Wix site it
# replaced is gone, and the site is live to the public.
#
# The site-wide X-Robots-Tag: noindex that guarded the pre-launch staging
# subdomain HAS BEEN REMOVED. It was deliberately fail-closed, so it followed
# the cutover onto the production domain and was suppressing indexing of the
# real site until it was taken out here.
#
# Do NOT reintroduce a global noindex. If a future staging environment needs
# one, note that Netlify does not support context-specific headers in
# netlify.toml -- [context.production.headers] and friends parse without error
# and are silently ignored (headers and redirects here are global; only build
# settings and environment variables are context-aware). Use Netlify's
# per-site "Prevent search engine indexing" setting on the staging site
# instead, so the guard cannot ride along to production.
# ---------------------------------------------------------------------------
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# ---------------------------------------------------------------------------
# REDIRECTS
# ---------------------------------------------------------------------------

# Preserve the old (misspelled) live-site URL for inbound links & SEO.
[[redirects]]
  from = "/ankle-franctures"
  to = "/ankle-fractures.html"
  status = 301

# Ogden consolidation. ogden.html and ogden-utah-foot-doctor.html both targeted
# Ogden podiatry intent; ogden.html was orphaned and its own schema sameAs
# pointed at the other page. The city page is now ogden-utah.html (serving
# Ogden) and the clinic page remains ogden-utah-foot-doctor.html (the South
# Ogden address). ogden.html is retired.
[[redirects]]
  from = "/ogden.html"
  to = "/ogden-utah.html"
  status = 301

[[redirects]]
  from = "/ogden"
  to = "/ogden-utah.html"
  status = 301

# The old locations entry point was the Farmington page, which made Farmington
# the de facto index and left every other city unreachable. /locations is now
# the hub; Farmington keeps its own URL as a clinic page.
[[redirects]]
  from = "/locations"
  to = "/locations.html"
  status = 301

# Extensionless convenience URLs for the city pages, so inbound links, print,
# and Google Business Profile references work with or without the .html suffix.
[[redirects]]
  from = "/kaysville"
  to = "/kaysville-utah.html"
  status = 301
[[redirects]]
  from = "/fruit-heights"
  to = "/fruit-heights-utah.html"
  status = 301
[[redirects]]
  from = "/centerville"
  to = "/centerville-utah.html"
  status = 301
[[redirects]]
  from = "/bountiful"
  to = "/bountiful-utah.html"
  status = 301
[[redirects]]
  from = "/woods-cross"
  to = "/woods-cross-utah.html"
  status = 301
[[redirects]]
  from = "/layton"
  to = "/layton-utah.html"
  status = 301
[[redirects]]
  from = "/clearfield"
  to = "/clearfield-utah.html"
  status = 301
[[redirects]]
  from = "/roy"
  to = "/roy-utah.html"
  status = 301
[[redirects]]
  from = "/riverdale"
  to = "/riverdale-utah.html"
  status = 301
[[redirects]]
  from = "/north-ogden"
  to = "/north-ogden-utah.html"
  status = 301
[[redirects]]
  from = "/farmington"
  to = "/farmington-utah.html"
  status = 301
[[redirects]]
  from = "/south-ogden"
  to = "/ogden-utah-foot-doctor.html"
  status = 301

# NOTE: the Wix -> Netlify 301 map for the domain cutover is NOT here yet. It
# cannot be written without crawling the live Wix site for its indexed URL set.
# That is Phase 0, items 1-2 in SEO-AUDIT-AND-LOCAL-PLAN.md, and it must be
# completed before DNS is pointed at Netlify.
